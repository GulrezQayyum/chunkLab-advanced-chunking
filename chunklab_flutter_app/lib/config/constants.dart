class AppConstants {
  static const String apiBaseUrl = 'http://localhost:8001';
  static const String queryEndpoint = '/api/documents/query';
  static const Duration apiTimeout = Duration(seconds: 30);
}